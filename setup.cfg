[metadata]
name = fierte_pre_commit_hooks
version = 1.0.0
description = hooks for Python tool "pre-commit".

[options]
packages = find:

[options.entry_points]
console_scripts =
    prefix_and_issue_num = fierte_pre_commit_hooks.prefix_and_issue_num:main